# Handoff Summary — Bribe The Band → Claude Code

Written 2026-08-25, at the point Travis switched this project from Cowork/chat into Claude Code so Claude could get direct GitHub repo access. **Updated 2026-08-27 — production (`bribetheband.live`) is now the live, real-world app**: real Stripe payments, queue boosting, post-play cooldown, and a per-gig performer display-name override are all live and have been used at a real gig with real tips and requests. **Fully rewritten 2026-09-13 — see "Staging vs. production" below**: a full, from-scratch pass consolidating everything built since 2026-09-09, including a StoryBrand rewrite of the marketing site, Stage Mode, per-code fee discounts, affiliate-link attribution, and the Owner Console (now considered done for now, in its own separate repo). Production and staging are fully in sync as of this update. Also worth knowing: this doc and its siblings can go stale relative to the actual repo, and did — see the 2026-09-08 session entry in `04-DECISIONS-AND-OPEN-QUESTIONS.md` for two things that had been wrong here for weeks (SMTP, self-service signup) until directly re-verified. Read this first, then `01-ARCHITECTURE-AND-DATA-MODEL.md` and `04-DECISIONS-AND-OPEN-QUESTIONS.md` for full depth.

## What this project is
"Bribe The Band" (bribetheband.live) — a song-request-and-tip web app. Audience members browse a performer's song catalog, request songs with an optional tip, and the performer runs a live "console + chart viewer" during the show to accept requests and splice them into the live set. Multi-tenant: any performer can sign up and get their own hosted page. Travis Ross performs as "Ten Cent Prophet"; his own account (`travis-ross`) is the real, live account, used as the de facto testing ground throughout the build.

Stack: **Supabase** (Postgres + Auth + RLS) for the backend, **Vercel** (static hosting + serverless functions) for the app, **Wasabi** (S3-compatible) for chart PDF storage. GitHub repo: `travis-ross-73/bribe-the-band`.

Full architecture, data model, and feature history are in `docs/01-ARCHITECTURE-AND-DATA-MODEL.md` and `docs/04-DECISIONS-AND-OPEN-QUESTIONS.md` — those two are the most important files in this folder if you need deep context on how anything works or why a decision was made.

## Repo access — confirmed working
Every prior session (running in Cowork/chat, not Claude Code) hit a sandbox restriction: `git push origin main` always failed with a 403 ("not in this session's authorized repository set"). Every deliverable had to be committed locally, then handed to Travis as a downloadable file for him to manually upload via GitHub's web UI. **Confirmed 2026-08-25: Claude Code has direct push access** to `travis-ross-73/bribe-the-band` (Travis provided a GitHub fine-grained PAT scoped to this repo; verified with a real test push/delete before doing anything real). Clone the repo locally (e.g. `/Users/Rossomeness/Claude/bribe-the-band`) and work from there — don't assume a clone already exists at session start, since this is a fresh Claude Code environment each time.

## Exactly where things stand right now — production is live and in real use
`bribetheband.live` is the real, live app, confirmed working at a real Travis gig on 2026-08-27 with real crowd tips and requests. Staging (a second, fully isolated copy of the whole stack) still exists and is the standing pattern for building/testing anything new before it reaches production — see `01-ARCHITECTURE-AND-DATA-MODEL.md` and `04-DECISIONS-AND-OPEN-QUESTIONS.md` for full feature-by-feature detail; `07-STAGING-ENVIRONMENT-SETUP.md` covers the staging build itself (now historical — treat it as "how staging was built," not "what's newest").

**Environments (current):**
| | Production | Staging |
|---|---|---|
| Supabase URL | `ykvpjeiakvgihpxektcf.supabase.co` | `orwxehvthwflgoqnbafp.supabase.co` |
| Wasabi bucket | `songchart` | `songchart-staging` |
| GitHub branch | `main` | `staging` |
| Vercel project | `bribe-the-band` (`bribetheband.live`) | `bribe-the-band-staging` (`bribe-the-band-staging-three.vercel.app`) |
| Stripe | **live-mode**, verified in real use | test-mode, verified end-to-end |

**What's live in production, as of 2026-08-27**:
- Real Stripe payments (Payment Element, live keys, Apple Pay on both `bribetheband.live` and `www.bribetheband.live`) — built/verified on staging 2026-08-26, rolled out 2026-08-27.
- Queue boosting (add a tip to an already-requested song to move it up) and post-play cooldown (a song can't be re-requested for N minutes after the performer taps "Got it — play the song") — built/verified on staging, rolled out to production the same day.
- A per-gig performer display-name override, settable in the console, shown on the crowd page — defaults to the performer's account name if left blank.
- Console usability pass: clearer labeling on the two main gig-control buttons ("Step 1 — Go Live" / "Step 2 — Perform").

**Reusable pattern for porting staging → production, worth knowing before touching either branch**: never `git merge staging` into `main` — it would pull staging's Supabase project and Stripe test-mode key into production. Diff `staging` against its own pre-change state, apply that as a standalone patch onto a branch off `main`, and grep the result to confirm every environment-specific line (Supabase URL/key, Stripe publishable key) is untouched before pushing. Run any new migration against production *before* pushing the matching code.

**Staging vs. production, as of 2026-09-13** — fully in sync (aside from the standing, intentional split: `docs/` and a handful of already-run migration `.sql` files only live on `main`, per the branch doctrine above). Everything below is live on both `bribetheband.live` and `bribe-the-band-staging-three.vercel.app` unless noted otherwise.

**The marketing site** (`index.html`, `how-it-works.html`, `features.html`, `get-started.html`) was rebuilt twice in close succession:
- First, from scratch in the House Lights Down visual design (2026-09-11), replacing the long-standing placeholder homepage — see items 32/38/39.
- Then rewritten again around a **StoryBrand narrative** (2026-09-12/13, see item 49): the performer is the hero, BTB is the guide. Home now opens with the actual problem (asked to play a song you don't know, or scrambling to find the chart for one you do, shouting titles across the stage) before the plan; success is framed as the crowd *participating* in the show, not the performer "running" it. How It Works expanded from 3 to 5 steps (adding Band Sync and Last Call as their own steps). The old standalone `/highlights` page (item 45, built 2026-09-11) was folded entirely into The Product page rather than staying a separate nav tab — `/highlights` is now a permanent redirect to `/features`. Nav CTA renamed to "Create your gig page," "Performer log in" demoted to a small secondary "Log in" link.
- **Affiliate-link attribution** (item 46) rides along on these same pages: a `?ref=`/`?code=` URL param now sets a 30-day `btb_ref` cookie, so `signup.html` can still pre-fill the promo code even if the visitor browses the marketing site first or comes back later — previously this only worked if signup happened in the very same page load as the link click.

**The crowd page, console, and chart viewer** (`request.html`, `console.html`, `viewer.html`) also got the House Lights Down reskin (item 42) — same visual system as the marketing pages, every JS-referenced element id/class preserved. The chart viewer additionally gained a **Stage Mode** toggle (item 44): inverted colors (black background, white text) for low-light stage reading, remembered per device via `localStorage`.

**Money-related features, all live**: Stripe Connect Express (items 36/37 — per-performer payouts, Travis's live-mode Stripe account fully enrolled as a Connect platform) and, new this week, **promo codes that temporarily discount a performer's platform fee_percentage** (item 47 — e.g. "first 100 signups pay 8% instead of 10%," with the affiliate's own commission following along automatically since it's computed off whatever the platform actually collects).

**The Owner Console** (`10-OWNER-CONSOLE-SCOPE.md`) — a local-only admin tool in its own separate repo (`bribe-the-band-owner-console`), never publicly deployed — was scoped, built, and verified against real data in both environments across 2026-09-11/12 (item 43), followed by a round of real-world bug fixes (item 48: a stale/confusing subscription-discount field disabled, a real layout bug in the promo-code panel fixed, a robustness gap where any unhandled error could crash the whole local server rather than just one request, and a bug where "Copy Link" always copied the production URL regardless of which environment was selected). **This work is done and considered final for now** — the separate repo/session that built it has wrapped up; further changes to the Owner Console are a deliberate later decision, not an open thread.

Also still true: chart PDFs are optional when adding a song, forgot-password/change-password both work, the Follower account-growth model is one identity mechanism, 12-hour gig auto-end, Last Call, custom SMTP, self-service signup, and the console's info-icon/Settings-tab/CSV-export cleanup are all live on both environments — see `04-DECISIONS-AND-OPEN-QUESTIONS.md`'s 2026-09-09/10 sessions for that history. A real production login (`travis-ross-test`) exists for Claude Code to test auth-gated actions hands-on. Full detail across all of the above: `01-ARCHITECTURE-AND-DATA-MODEL.md` and `04-DECISIONS-AND-OPEN-QUESTIONS.md`.

**A note on how this last batch of work happened**: for a stretch around 2026-09-11 through -13, Travis ran two Claude Code sessions against this same local clone at once — this one, and a separate thread scoping/building the Owner Console. Both sessions read and wrote the same working directory and git branches, which caused some real friction: an uncommitted change in one session's working tree was silently overwritten more than once when the other session ran a `git checkout` (confirmed side-effect, not a bug in either session's own logic) — content was recovered by re-applying from a source that still had it (usually the `staging` branch itself), and a spot-check afterward confirmed nothing from either thread's actual work was permanently lost. **Going forward, treat concurrent Claude Code sessions against the same local clone as genuinely risky** — prefer a second clone/worktree for parallel work on this repo, the way the Owner Console's separate repo already avoided the problem for its own codebase.

**What's left**: nothing urgent blocking real use — see `03-V2-BACKLOG.md` for deferred ideas and `04-DECISIONS-AND-OPEN-QUESTIONS.md`'s "Open questions" section for anything still undecided. **Travis's own standing direction as of 2026-09-08**: don't wait on Wade to test staging before continuing — he isn't available and further building shouldn't be gated on that for anything that isn't a payments/security-review item.

## One thread from last time — now resolved
Travis had mentioned waiting on "admin to re-enable access," which was never clarified in the moment. **Resolved**: this referred to Travis's own Claude subscription access, not anything in this project — a billing/credit-card issue that the org's admin fixed. Not staging-related; no longer worth tracking here.

## Docs in this folder
Originally exported from the claude.ai Project "Song Request and Tip App," then kept locally in Dropbox, then **moved into this git repo's `docs/` folder 2026-08-29** — see the note below. All numbered docs plus this summary:
- `00-PROJECT-OVERVIEW.md` — original prototype-era overview (superseded in substance by `01-...`, kept for history)
- `01-ARCHITECTURE-AND-DATA-MODEL.md` — **the most important file**: full current architecture, data model, every feature built and why, Wade's security doctrine
- `02-SONG-DATABASE.md` — where Travis's chart library lives, the 50-song approved list
- `03-V2-BACKLOG.md` — deferred features, not yet prioritized
- `04-DECISIONS-AND-OPEN-QUESTIONS.md` — **second most important file**: full session-by-session history of every decision, bug found, and fix, in chronological order
- `05-SIGNUP-SCOPE.md` — self-service signup: Phase 1 (free signup) **confirmed deployed and working on both environments 2026-09-08**; billing/affiliate payouts/device enforcement still not built (see that doc for phase detail)
- `06-OWNERS-GUIDE-NOTES.md` — outline for an eventual "how Travis runs the business" ops manual (not written yet) — much of this is expected to shrink once the Owner Console (item 10 below) exists
- `07-STAGING-ENVIRONMENT-SETUP.md` — the staging build currently in progress (see above)
- `08-PERFORMER-FAQ-TUTORIAL-NOTES.md` — running list of customer-facing FAQ/tutorial topics to write later. **Largely superseded by `FAQ-PAGE-COPY.md` below (added 2026-09-13)** — check that file first before treating a topic here as unwritten.
- `09-CONNECT-EXPRESS-SCOPE.md` — Stripe Connect Express plan (self-serve per-performer payouts, the 10% platform fee) — **built, tested, and ported to production 2026-09-11**
- `10-OWNER-CONSOLE-SCOPE.md` — a local-only (never publicly deployed) admin tool: performer/affiliate management, cross-performer revenue and affiliate-commission reporting, pulling from both Supabase (via service_role) and Stripe — **built and verified in both environments, 2026-09-11/12**, in its own separate repo (`bribe-the-band-owner-console`)
- `11-CHART-APP-IMPORT-SCOPE.md` — **added 2026-09-13** (research complete, nothing built). How a new performer gets an existing chart library into BTB from Ultimate Guitar or Songbook Pro. Headline finding: Songbook Pro's `.sbpbackup` is a fully-documented ZIP of JSON — title/artist/key/capo/setlists all come out cleanly, a genuine one-file importer. Ultimate Guitar has **no library or playlist export at all** — only a narrow "download tabs you personally edited" path, desktop-browser-only, no key metadata, ever. Recommends building the Songbook Pro importer properly and giving UG users a manual tutorial rather than an integration. Also where the patent-risk finding below originally surfaced (a competitive scan side-effect, unrelated to this doc's own subject).
- `12-PATENT-RISK-ASSESSMENT.md` — **added 2026-09-13**. A published (not granted) US patent application, US 2023/0297898 A1, whose single claim maps closely — limitation by limitation — onto BTB's tip-ranked request/boost/accept flow. Not currently enforceable (a published application isn't an issued patent), and its own status is ambiguous between sources (Justia says pending, Google Patents' automated flag says abandoned). **Recommendation: get a real USPTO status check and a short freedom-to-operate opinion from a patent attorney** — this is flagged as worth real legal attention, not a reason to stop building in the meantime.
- `13-SITE-AUDIT-AND-COMPLIANCE.md` — **added 2026-09-13**, a cold outside audit of the live marketing/signup site: legal/compliance gaps (no Terms/Privacy/Refund policy, no legal entity, chart-storage exposure), a confirmed `/demo` bug, fee-disclosure and copy issues, and a ranked action plan (Tier 0 through Tier 3). Nothing in Tier 0 has been actioned yet — see item 50 in `04-DECISIONS-AND-OPEN-QUESTIONS.md`.
- `LEGAL-DRAFT-terms-of-service.md`, `LEGAL-DRAFT-privacy-policy.md`, `LEGAL-DRAFT-refund-and-request-policy.md` — **added 2026-09-13**, first-pass drafts for the Tier 0 legal gaps above. Explicitly marked "DRAFT FOR ATTORNEY REVIEW — DO NOT PUBLISH AS-IS" and written by an AI assistant, not a lawyer — every placeholder (`[ENTITY]`, `[EFFECTIVE DATE]`, etc.) and every **[VERIFY]**/**[ATTORNEY DECISION POINT]** flag must be resolved before any of these goes live.
- `FAQ-PAGE-COPY.md` — **added 2026-09-13**, ready-to-publish `/faq` copy (unlike the legal drafts, marked ready rather than attorney-gated) — six answers flagged **[CONFIRM]** where real app behavior needs verifying before publishing (phone/tablet support, chart file size/page limits, whether data export is actually manual, etc.). Deliberately keeps the honest "your venue's wifi might die, run a hotspot, keep a backup" answer rather than softening it.
- `PRICING-PAGE-COPY.md` — **added 2026-09-13**, ready-to-publish `/pricing` copy written at the **10% fee currently shipping**, plus an appendix modeling two alternatives (7% flat, and a notable 15%-all-in-absorbing-Stripe's-fee option that nets the performer *more* on any tip under ~$14.29 despite the higher headline number). **Open decision, not yet made**: which fee model to actually ship — see item 51.
- `2-hour-setlist.md` — a sample setlist built from the approved song database
- `gig-request-prototype.html` — the very first single-file prototype, superseded, kept only as a UX reference

## Where these docs actually live now
**Decided 2026-08-29**: `docs/` moved into this git repo — it's the shared source of truth now, committed alongside the code, not a separate manually-synced copy in Dropbox or a claude.ai Project. `docs/` exists only on `main` (and branches cut from it) — it was never merged onto `staging`; read it via `git show main:docs/<file>` while working there. See `CLAUDE.md` for the full working rules (pull-before-editing, commit-message conventions, etc.).

**The claude.ai Project's own Knowledge tab is a separate, second copy** — what Cowork sessions read from, manually re-uploaded by Travis after a round of edits here. It only stays current when he remembers to refresh it; Claude Code has no way to write to it directly. `04-DECISIONS-AND-OPEN-QUESTIONS.md`'s 2026-08-29 entry ("Docs live in two places, on purpose") has the full reasoning for why this split is intentional rather than a bug.
